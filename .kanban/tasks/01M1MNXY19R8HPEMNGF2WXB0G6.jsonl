{"id":"01M1MNXY1AWYYVB4F10ESVYPCC","timestamp":"2026-09-03T22:23:46.730166Z","op":"create","item_id":"01M1MNXY19R8HPEMNGF2WXB0G6","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,59 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8280'\n+title: 'Terminal output spike: prove Noora on macOS 27, or choose the ANSI fallback'\n+---\n+## What\n+\n+cli-plan.md §5.2. This is a spike, and not an adoption. It decides which\n+package draws the spinner, the progress bar and the table that the\n+`doctor`, `config` and progress work all need.\n+\n+No package in this family uses a terminal UI library today, so there is\n+no precedent and no migration cost.\n+\n+Build a throwaway executable in the scratch directory (NOT in this\n+repository's `Sources/`) that:\n+\n+1. Declares `github.com/tuist/Noora` and builds on macOS 27 with\n+   swift-tools-version 6.2.\n+2. Draws one spinner, one progress bar that a loop advances, and one\n+   three-column table.\n+3. Writes every one of them to **stderr**, and writes nothing when\n+   stderr is not a terminal.\n+\n+Report the result in a comment on this task: does it build, does it fit,\n+and what is its dependency tail?\n+\n+If Noora does not build, does not support macOS 27, or brings a large\n+dependency tail, choose the fallback: about 200 lines of our own ANSI in\n+`Sources/acp-agent/Terminal/`. We need a spinner, a bar, a table and\n+three colors, and nothing more.\n+\n+- [ ] Declare Noora in a scratch package and build it on macOS 27\n+- [ ] Draw a spinner, a bar and a table\n+- [ ] Prove the stderr-only and not-a-terminal rules\n+- [ ] Record the verdict and the dependency tail in a task comment\n+- [ ] If the verdict is no, write the ANSI fallback design in the comment\n+\n+## Acceptance Criteria\n+\n+- [ ] A task comment states the verdict, the Noora version tried, the\n+      dependency tail, and the reason.\n+- [ ] The chosen approach draws nothing at all when stderr is a pipe.\n+- [ ] The chosen approach never writes to stdout.\n+- [ ] No scratch code lands in this repository. The result of this task\n+      is the decision, plus the comment.\n+\n+## Tests\n+\n+- [ ] `Tests/FoundationModelsACPAgentTests/TerminalOutputTests.swift`:\n+      one test that pins the decision — the renderer chosen by this\n+      spike writes nothing when its destination is not a terminal. It\n+      uses a `Pipe`, and it asserts the captured bytes are empty.\n+- [ ] `swift test --filter TerminalOutputTests` passes.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,59 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8280'\n-title: 'Terminal output spike: prove Noora on macOS 27, or choose the ANSI fallback'\n----\n-## What\n-\n-cli-plan.md §5.2. This is a spike, and not an adoption. It decides which\n-package draws the spinner, the progress bar and the table that the\n-`doctor`, `config` and progress work all need.\n-\n-No package in this family uses a terminal UI library today, so there is\n-no precedent and no migration cost.\n-\n-Build a throwaway executable in the scratch directory (NOT in this\n-repository's `Sources/`) that:\n-\n-1. Declares `github.com/tuist/Noora` and builds on macOS 27 with\n-   swift-tools-version 6.2.\n-2. Draws one spinner, one progress bar that a loop advances, and one\n-   three-column table.\n-3. Writes every one of them to **stderr**, and writes nothing when\n-   stderr is not a terminal.\n-\n-Report the result in a comment on this task: does it build, does it fit,\n-and what is its dependency tail?\n-\n-If Noora does not build, does not support macOS 27, or brings a large\n-dependency tail, choose the fallback: about 200 lines of our own ANSI in\n-`Sources/acp-agent/Terminal/`. We need a spinner, a bar, a table and\n-three colors, and nothing more.\n-\n-- [ ] Declare Noora in a scratch package and build it on macOS 27\n-- [ ] Draw a spinner, a bar and a table\n-- [ ] Prove the stderr-only and not-a-terminal rules\n-- [ ] Record the verdict and the dependency tail in a task comment\n-- [ ] If the verdict is no, write the ANSI fallback design in the comment\n-\n-## Acceptance Criteria\n-\n-- [ ] A task comment states the verdict, the Noora version tried, the\n-      dependency tail, and the reason.\n-- [ ] The chosen approach draws nothing at all when stderr is a pipe.\n-- [ ] The chosen approach never writes to stdout.\n-- [ ] No scratch code lands in this repository. The result of this task\n-      is the decision, plus the comment.\n-\n-## Tests\n-\n-- [ ] `Tests/FoundationModelsACPAgentTests/TerminalOutputTests.swift`:\n-      one test that pins the decision — the renderer chosen by this\n-      spike writes nothing when its destination is not a terminal. It\n-      uses a `Pipe`, and it asserts the captured bytes are empty.\n-- [ ] `swift test --filter TerminalOutputTests` passes.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M1MP5RVW590F780J952ZKCC3","timestamp":"2026-09-03T22:28:03.580782Z","op":"update","item_id":"01M1MNXY19R8HPEMNGF2WXB0G6","forward_patch":"--- original\n+++ modified\n@@ -3,57 +3,63 @@\n - claude-code\n position_column: todo\n position_ordinal: '8280'\n-title: 'Terminal output spike: prove Noora on macOS 27, or choose the ANSI fallback'\n+title: Adopt Noora as the terminal renderer for both CLIs\n ---\n-## What\n+### What\n\n-cli-plan.md §5.2. This is a spike, and not an adoption. It decides which\n-package draws the spinner, the progress bar and the table that the\n-`doctor`, `config` and progress work all need.\n+cli-plan.md §5.2. The bar is a good Rust CLI. That stack is clap,\n+indicatif, dialoguer, comfy-table and owo-colors. In Swift,\n+swift-argument-parser covers clap, and **Noora** (Tuist) covers the\n+other four in one CLI design system: spinners, progress, tables and\n+alerts.\n\n-No package in this family uses a terminal UI library today, so there is\n-no precedent and no migration cost.\n+Adopt it. No package in the family uses a terminal UI library today, so\n+this task sets the family precedent, and the client CLI follows it.\n\n-Build a throwaway executable in the scratch directory (NOT in this\n-repository's `Sources/`) that:\n+1. Declare `https://github.com/tuist/Noora` in `Package.swift`, pinned\n+   to an exact version, and link it into the `acp-agent` target.\n+2. Write `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one\n+   place any CLI code draws. It vends three things, and nothing more:\n+   - a spinner,\n+   - a progress bar that takes a fraction and a byte pair,\n+   - a table of rows with a status column.\n+3. **The absolute rule:** the renderer writes to **stderr** only, and it\n+   draws nothing at all when stderr is not a terminal. It never touches\n+   stdout. Take the destination as a `FileHandle` in the initializer, so\n+   a test injects a `Pipe`.\n+4. Take the terminal test with `isatty` on the injected descriptor, and\n+   not on the process global, so a test can drive both paths.\n\n-1. Declares `github.com/tuist/Noora` and builds on macOS 27 with\n-   swift-tools-version 6.2.\n-2. Draws one spinner, one progress bar that a loop advances, and one\n-   three-column table.\n-3. Writes every one of them to **stderr**, and writes nothing when\n-   stderr is not a terminal.\n+Every later task that draws — the download progress, `doctor`, and\n+`config path` — calls this type. None of them imports Noora directly.\n+That keeps the swap cost at one file if Noora ever disappoints.\n\n-Report the result in a comment on this task: does it build, does it fit,\n-and what is its dependency tail?\n-\n-If Noora does not build, does not support macOS 27, or brings a large\n-dependency tail, choose the fallback: about 200 lines of our own ANSI in\n-`Sources/acp-agent/Terminal/`. We need a spinner, a bar, a table and\n-three colors, and nothing more.\n-\n-- [ ] Declare Noora in a scratch package and build it on macOS 27\n-- [ ] Draw a spinner, a bar and a table\n-- [ ] Prove the stderr-only and not-a-terminal rules\n-- [ ] Record the verdict and the dependency tail in a task comment\n-- [ ] If the verdict is no, write the ANSI fallback design in the comment\n+- [ ] Declare and pin Noora, and link it\n+- [ ] `TerminalRenderer`: spinner, bar, table\n+- [ ] stderr only, and silent when not a terminal\n+- [ ] No other file in `Sources/acp-agent/` imports Noora\n\n-## Acceptance Criteria\n+### Acceptance Criteria\n\n-- [ ] A task comment states the verdict, the Noora version tried, the\n-      dependency tail, and the reason.\n-- [ ] The chosen approach draws nothing at all when stderr is a pipe.\n-- [ ] The chosen approach never writes to stdout.\n-- [ ] No scratch code lands in this repository. The result of this task\n-      is the decision, plus the comment.\n+- [ ] `swift build` succeeds on macOS 27 with Noora linked.\n+- [ ] The renderer writes zero bytes when its destination is a `Pipe`.\n+- [ ] The renderer never writes to file descriptor 1.\n+- [ ] `TerminalRenderer.swift` is the only file in `Sources/acp-agent/`\n+      that holds `import Noora`.\n+- [ ] The spinner, the bar and the table each draw something when the\n+      destination is a terminal.\n\n-## Tests\n+### Tests\n\n-- [ ] `Tests/FoundationModelsACPAgentTests/TerminalOutputTests.swift`:\n-      one test that pins the decision — the renderer chosen by this\n-      spike writes nothing when its destination is not a terminal. It\n-      uses a `Pipe`, and it asserts the captured bytes are empty.\n-- [ ] `swift test --filter TerminalOutputTests` passes.\n+- [ ] `Tests/FoundationModelsACPAgentTests/TerminalRendererTests.swift`:\n+      with a `Pipe` destination, each of the three renderers writes zero\n+      bytes.\n+- [ ] A test captures stdout during a full render and asserts it is\n+      empty.\n+- [ ] A source-level test asserts that `import Noora` appears in exactly\n+      one file under `Sources/acp-agent/`. It pins the swap cost.\n+- [ ] `swift test --filter TerminalRendererTests` passes.\n+- [ ] `swift build` passes with no warning.\n\n-## Workflow\n+### Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,63 +3,57 @@\n - claude-code\n position_column: todo\n position_ordinal: '8280'\n-title: Adopt Noora as the terminal renderer for both CLIs\n+title: 'Terminal output spike: prove Noora on macOS 27, or choose the ANSI fallback'\n ---\n-### What\n+## What\n\n-cli-plan.md §5.2. The bar is a good Rust CLI. That stack is clap,\n-indicatif, dialoguer, comfy-table and owo-colors. In Swift,\n-swift-argument-parser covers clap, and **Noora** (Tuist) covers the\n-other four in one CLI design system: spinners, progress, tables and\n-alerts.\n+cli-plan.md §5.2. This is a spike, and not an adoption. It decides which\n+package draws the spinner, the progress bar and the table that the\n+`doctor`, `config` and progress work all need.\n\n-Adopt it. No package in the family uses a terminal UI library today, so\n-this task sets the family precedent, and the client CLI follows it.\n+No package in this family uses a terminal UI library today, so there is\n+no precedent and no migration cost.\n\n-1. Declare `https://github.com/tuist/Noora` in `Package.swift`, pinned\n-   to an exact version, and link it into the `acp-agent` target.\n-2. Write `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one\n-   place any CLI code draws. It vends three things, and nothing more:\n-   - a spinner,\n-   - a progress bar that takes a fraction and a byte pair,\n-   - a table of rows with a status column.\n-3. **The absolute rule:** the renderer writes to **stderr** only, and it\n-   draws nothing at all when stderr is not a terminal. It never touches\n-   stdout. Take the destination as a `FileHandle` in the initializer, so\n-   a test injects a `Pipe`.\n-4. Take the terminal test with `isatty` on the injected descriptor, and\n-   not on the process global, so a test can drive both paths.\n+Build a throwaway executable in the scratch directory (NOT in this\n+repository's `Sources/`) that:\n+\n+1. Declares `github.com/tuist/Noora` and builds on macOS 27 with\n+   swift-tools-version 6.2.\n+2. Draws one spinner, one progress bar that a loop advances, and one\n+   three-column table.\n+3. Writes every one of them to **stderr**, and writes nothing when\n+   stderr is not a terminal.\n+\n+Report the result in a comment on this task: does it build, does it fit,\n+and what is its dependency tail?\n\n-Every later task that draws — the download progress, `doctor`, and\n-`config path` — calls this type. None of them imports Noora directly.\n-That keeps the swap cost at one file if Noora ever disappoints.\n+If Noora does not build, does not support macOS 27, or brings a large\n+dependency tail, choose the fallback: about 200 lines of our own ANSI in\n+`Sources/acp-agent/Terminal/`. We need a spinner, a bar, a table and\n+three colors, and nothing more.\n\n-- [ ] Declare and pin Noora, and link it\n-- [ ] `TerminalRenderer`: spinner, bar, table\n-- [ ] stderr only, and silent when not a terminal\n-- [ ] No other file in `Sources/acp-agent/` imports Noora\n+- [ ] Declare Noora in a scratch package and build it on macOS 27\n+- [ ] Draw a spinner, a bar and a table\n+- [ ] Prove the stderr-only and not-a-terminal rules\n+- [ ] Record the verdict and the dependency tail in a task comment\n+- [ ] If the verdict is no, write the ANSI fallback design in the comment\n\n-### Acceptance Criteria\n+## Acceptance Criteria\n\n-- [ ] `swift build` succeeds on macOS 27 with Noora linked.\n-- [ ] The renderer writes zero bytes when its destination is a `Pipe`.\n-- [ ] The renderer never writes to file descriptor 1.\n-- [ ] `TerminalRenderer.swift` is the only file in `Sources/acp-agent/`\n-      that holds `import Noora`.\n-- [ ] The spinner, the bar and the table each draw something when the\n-      destination is a terminal.\n+- [ ] A task comment states the verdict, the Noora version tried, the\n+      dependency tail, and the reason.\n+- [ ] The chosen approach draws nothing at all when stderr is a pipe.\n+- [ ] The chosen approach never writes to stdout.\n+- [ ] No scratch code lands in this repository. The result of this task\n+      is the decision, plus the comment.\n\n-### Tests\n+## Tests\n\n-- [ ] `Tests/FoundationModelsACPAgentTests/TerminalRendererTests.swift`:\n-      with a `Pipe` destination, each of the three renderers writes zero\n-      bytes.\n-- [ ] A test captures stdout during a full render and asserts it is\n-      empty.\n-- [ ] A source-level test asserts that `import Noora` appears in exactly\n-      one file under `Sources/acp-agent/`. It pins the swap cost.\n-- [ ] `swift test --filter TerminalRendererTests` passes.\n-- [ ] `swift build` passes with no warning.\n+- [ ] `Tests/FoundationModelsACPAgentTests/TerminalOutputTests.swift`:\n+      one test that pins the decision — the renderer chosen by this\n+      spike writes nothing when its destination is not a terminal. It\n+      uses a `Pipe`, and it asserts the captured bytes are empty.\n+- [ ] `swift test --filter TerminalOutputTests` passes.\n\n-### Workflow\n+## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M1MR94C2J7YPVX68Y7SMBGHA","timestamp":"2026-09-03T23:04:50.818190Z","op":"update","item_id":"01M1MNXY19R8HPEMNGF2WXB0G6","forward_patch":"--- original\n+++ modified\n@@ -1,63 +1,74 @@\n ---\n assignees:\n - claude-code\n+depends_on:\n+- 01M1MNYFW81216M57PS9NDZKBE\n position_column: todo\n position_ordinal: '8280'\n-title: Adopt Noora as the terminal renderer for both CLIs\n+title: Adopt Noora, and write the agent CLI's TerminalRenderer\n ---\n ### What\n\n-cli-plan.md §5.2. The bar is a good Rust CLI. That stack is clap,\n-indicatif, dialoguer, comfy-table and owo-colors. In Swift,\n-swift-argument-parser covers clap, and **Noora** (Tuist) covers the\n-other four in one CLI design system: spinners, progress, tables and\n-alerts.\n+cli-plan.md §5.2. swift-argument-parser covers clap; **Noora** (Tuist)\n+covers indicatif, dialoguer, comfy-table and owo-colors in one CLI\n+design system. Adopt it.\n\n-Adopt it. No package in the family uses a terminal UI library today, so\n-this task sets the family precedent, and the client CLI follows it.\n+This card delivers the **agent** package's renderer only. The client\n+package declares and writes its own, in its N1 card, because the two\n+packages must not depend on each other.\n\n 1. Declare `https://github.com/tuist/Noora` in `Package.swift`, pinned\n-   to an exact version, and link it into the `acp-agent` target.\n-2. Write `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one\n-   place any CLI code draws. It vends three things, and nothing more:\n-   - a spinner,\n-   - a progress bar that takes a fraction and a byte pair,\n-   - a table of rows with a status column.\n-3. **The absolute rule:** the renderer writes to **stderr** only, and it\n-   draws nothing at all when stderr is not a terminal. It never touches\n-   stdout. Take the destination as a `FileHandle` in the initializer, so\n-   a test injects a `Pipe`.\n-4. Take the terminal test with `isatty` on the injected descriptor, and\n-   not on the process global, so a test can drive both paths.\n+   to an **exact version**. Resolve the newest release that builds on\n+   macOS 27 with swift-tools-version 6.2, and write that version into\n+   `Package.swift` and into a task comment, so the client package pins\n+   the same one.\n+2. `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one place\n+   any CLI code draws. It vends three things and nothing more: a\n+   spinner, a progress bar that takes a fraction and a byte pair, and a\n+   table of rows with a status column.\n+3. **Both the destination and the terminal test are injected**, not read\n+   from process globals:\n+   ```swift\n+   init(destination: FileHandle, isTerminal: Bool)\n+   ```\n+   The production call site passes `.standardError` and\n+   `isatty(STDERR_FILENO) == 1`. A test drives **both** paths with a\n+   `Pipe` and an explicit boolean, so the terminal path is automated and\n+   needs no person to look at a screen.\n+4. The renderer writes to its destination only. It never touches file\n+   descriptor 1.\n\n-Every later task that draws — the download progress, `doctor`, and\n-`config path` — calls this type. None of them imports Noora directly.\n-That keeps the swap cost at one file if Noora ever disappoints.\n+Every later task that draws — the download progress, `doctor` and the\n+`--verbose` events — calls this type. None imports Noora.\n\n-- [ ] Declare and pin Noora, and link it\n-- [ ] `TerminalRenderer`: spinner, bar, table\n-- [ ] stderr only, and silent when not a terminal\n-- [ ] No other file in `Sources/acp-agent/` imports Noora\n+- [ ] Declare and pin Noora at an exact version; record the version\n+- [ ] `TerminalRenderer`, with the injected destination and terminal flag\n+- [ ] Spinner, bar and table\n+- [ ] No other file under `Sources/acp-agent/` imports Noora\n\n ### Acceptance Criteria\n\n-- [ ] `swift build` succeeds on macOS 27 with Noora linked.\n-- [ ] The renderer writes zero bytes when its destination is a `Pipe`.\n+- [ ] `swift build` succeeds on macOS 27 with Noora linked at the pinned\n+      version.\n+- [ ] With `isTerminal: false`, each of the three renderers writes zero\n+      bytes to the destination.\n+- [ ] With `isTerminal: true` and a `Pipe` destination, each of the\n+      three writes a non-empty payload.\n - [ ] The renderer never writes to file descriptor 1.\n-- [ ] `TerminalRenderer.swift` is the only file in `Sources/acp-agent/`\n-      that holds `import Noora`.\n-- [ ] The spinner, the bar and the table each draw something when the\n-      destination is a terminal.\n+- [ ] `TerminalRenderer.swift` is the only file under\n+      `Sources/acp-agent/` that holds `import Noora`.\n\n ### Tests\n\n - [ ] `Tests/FoundationModelsACPAgentTests/TerminalRendererTests.swift`:\n-      with a `Pipe` destination, each of the three renderers writes zero\n-      bytes.\n-- [ ] A test captures stdout during a full render and asserts it is\n-      empty.\n-- [ ] A source-level test asserts that `import Noora` appears in exactly\n-      one file under `Sources/acp-agent/`. It pins the swap cost.\n+      with `isTerminal: false` and a `Pipe`, the spinner, the bar and\n+      the table each write zero bytes.\n+- [ ] With `isTerminal: true` and a `Pipe`, each writes a non-empty\n+      payload. This automates the terminal path with no pseudo-terminal.\n+- [ ] A test captures file descriptor 1 during a full render and\n+      asserts it stayed empty.\n+- [ ] A source-level test asserts `import Noora` appears in exactly one\n+      file under `Sources/acp-agent/`.\n - [ ] `swift test --filter TerminalRendererTests` passes.\n - [ ] `swift build` passes with no warning.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -1,74 +1,63 @@\n ---\n assignees:\n - claude-code\n-depends_on:\n-- 01M1MNYFW81216M57PS9NDZKBE\n position_column: todo\n position_ordinal: '8280'\n-title: Adopt Noora, and write the agent CLI's TerminalRenderer\n+title: Adopt Noora as the terminal renderer for both CLIs\n ---\n ### What\n\n-cli-plan.md §5.2. swift-argument-parser covers clap; **Noora** (Tuist)\n-covers indicatif, dialoguer, comfy-table and owo-colors in one CLI\n-design system. Adopt it.\n+cli-plan.md §5.2. The bar is a good Rust CLI. That stack is clap,\n+indicatif, dialoguer, comfy-table and owo-colors. In Swift,\n+swift-argument-parser covers clap, and **Noora** (Tuist) covers the\n+other four in one CLI design system: spinners, progress, tables and\n+alerts.\n\n-This card delivers the **agent** package's renderer only. The client\n-package declares and writes its own, in its N1 card, because the two\n-packages must not depend on each other.\n+Adopt it. No package in the family uses a terminal UI library today, so\n+this task sets the family precedent, and the client CLI follows it.\n\n 1. Declare `https://github.com/tuist/Noora` in `Package.swift`, pinned\n-   to an **exact version**. Resolve the newest release that builds on\n-   macOS 27 with swift-tools-version 6.2, and write that version into\n-   `Package.swift` and into a task comment, so the client package pins\n-   the same one.\n-2. `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one place\n-   any CLI code draws. It vends three things and nothing more: a\n-   spinner, a progress bar that takes a fraction and a byte pair, and a\n-   table of rows with a status column.\n-3. **Both the destination and the terminal test are injected**, not read\n-   from process globals:\n-   ```swift\n-   init(destination: FileHandle, isTerminal: Bool)\n-   ```\n-   The production call site passes `.standardError` and\n-   `isatty(STDERR_FILENO) == 1`. A test drives **both** paths with a\n-   `Pipe` and an explicit boolean, so the terminal path is automated and\n-   needs no person to look at a screen.\n-4. The renderer writes to its destination only. It never touches file\n-   descriptor 1.\n+   to an exact version, and link it into the `acp-agent` target.\n+2. Write `Sources/acp-agent/Terminal/TerminalRenderer.swift` — the one\n+   place any CLI code draws. It vends three things, and nothing more:\n+   - a spinner,\n+   - a progress bar that takes a fraction and a byte pair,\n+   - a table of rows with a status column.\n+3. **The absolute rule:** the renderer writes to **stderr** only, and it\n+   draws nothing at all when stderr is not a terminal. It never touches\n+   stdout. Take the destination as a `FileHandle` in the initializer, so\n+   a test injects a `Pipe`.\n+4. Take the terminal test with `isatty` on the injected descriptor, and\n+   not on the process global, so a test can drive both paths.\n\n-Every later task that draws — the download progress, `doctor` and the\n-`--verbose` events — calls this type. None imports Noora.\n+Every later task that draws — the download progress, `doctor`, and\n+`config path` — calls this type. None of them imports Noora directly.\n+That keeps the swap cost at one file if Noora ever disappoints.\n\n-- [ ] Declare and pin Noora at an exact version; record the version\n-- [ ] `TerminalRenderer`, with the injected destination and terminal flag\n-- [ ] Spinner, bar and table\n-- [ ] No other file under `Sources/acp-agent/` imports Noora\n+- [ ] Declare and pin Noora, and link it\n+- [ ] `TerminalRenderer`: spinner, bar, table\n+- [ ] stderr only, and silent when not a terminal\n+- [ ] No other file in `Sources/acp-agent/` imports Noora\n\n ### Acceptance Criteria\n\n-- [ ] `swift build` succeeds on macOS 27 with Noora linked at the pinned\n-      version.\n-- [ ] With `isTerminal: false`, each of the three renderers writes zero\n-      bytes to the destination.\n-- [ ] With `isTerminal: true` and a `Pipe` destination, each of the\n-      three writes a non-empty payload.\n+- [ ] `swift build` succeeds on macOS 27 with Noora linked.\n+- [ ] The renderer writes zero bytes when its destination is a `Pipe`.\n - [ ] The renderer never writes to file descriptor 1.\n-- [ ] `TerminalRenderer.swift` is the only file under\n-      `Sources/acp-agent/` that holds `import Noora`.\n+- [ ] `TerminalRenderer.swift` is the only file in `Sources/acp-agent/`\n+      that holds `import Noora`.\n+- [ ] The spinner, the bar and the table each draw something when the\n+      destination is a terminal.\n\n ### Tests\n\n - [ ] `Tests/FoundationModelsACPAgentTests/TerminalRendererTests.swift`:\n-      with `isTerminal: false` and a `Pipe`, the spinner, the bar and\n-      the table each write zero bytes.\n-- [ ] With `isTerminal: true` and a `Pipe`, each writes a non-empty\n-      payload. This automates the terminal path with no pseudo-terminal.\n-- [ ] A test captures file descriptor 1 during a full render and\n-      asserts it stayed empty.\n-- [ ] A source-level test asserts `import Noora` appears in exactly one\n-      file under `Sources/acp-agent/`.\n+      with a `Pipe` destination, each of the three renderers writes zero\n+      bytes.\n+- [ ] A test captures stdout during a full render and asserts it is\n+      empty.\n+- [ ] A source-level test asserts that `import Noora` appears in exactly\n+      one file under `Sources/acp-agent/`. It pins the swap cost.\n - [ ] `swift test --filter TerminalRendererTests` passes.\n - [ ] `swift build` passes with no warning.\n\n","transaction_id":null}
