---
comments:
- actor: claude-code
  id: 01m1yfkynadbnrkdfgzmvy3c73
  text: |-
    Research and implementation notes.

    Read `.build/checkouts/FoundationModelsMultitool/Tests/Support/MCPTestServer/DynamicToolsetScenario.swift` for facts only. The scenario registers `counter` up front, then fires three stages, each one stage delay (1500 ms) after the one before it:
    1. add `greeter`, and send `tools/list_changed`;
    2. re-declare `counter` with a different `inputSchema`;
    3. remove `greeter`, and send `tools/list_changed`.

    So the mounted surface can hold two path states: `[counter]` and `[counter, greeter]`. The greeter comes in at stage 1 and goes out at stage 3, which gives two transitions to watch, not one.

    The old case read the surface one time and demanded that the greeter was absent at that moment. That read had to land before stage 1, which is a race against a 1500 ms wall-clock timer. A full `swift test` run loses that race.

    The new case makes no claim about which stage fired before its first read. It records whether the greeter is mounted, and then loops: read the surface with no turn boundary between the two reads and expect it did NOT move, run `turnWillBegin()`, read again. The loop ends when the greeter's state is the opposite of the state at the first read. Whichever stage fired first, the opposite state still comes.

    The staging property is what each iteration proves:
    - `#expect(betweenBoundaries == mounted)` — the mounted surface holds still between two turn boundaries, whatever the server sends. This is the "staged, not applied" half, and it is true whatever the machine's speed.
    - The loop ends only when a `turnWillBegin()` brings the change in. This is the "applies at the boundary" half.

    The assertion is stronger than the one it replaces: it compares the whole path list, in place of one `contains` test.

    Falsification check. With `await runCode.turnWillBegin()` deleted from the loop body, the case fails: "Test run with 1 test in 1 suite failed after 30.220 seconds with 1 issue". The turn boundary call is load-bearing. The line was then put back.

    No file of `FoundationModelsMultitool` or any other family package was changed.
  timestamp: 2026-09-07T17:45:52.554276+00:00
- actor: claude-code
  id: 01m1yfpk6f8yy7fm5feew90cya
  text: |-
    ### implement — changed
    - evidence: 1 file — `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift`. Three `swift test` runs in sequence, each "Test run with 428 tests in 45 suites passed with 1 known issue" — the known issue is `orderedSubsequenceAssertionChecksOrderWithGaps()` at `Tests/FoundationModelsACPAgentTests/Support/HarnessSmokeTests.swift:239`, and there is no other issue. `swift test --filter 'MCPCompositionTests/aToolListChangeIsStagedAndAppliesOnlyAtTheNextTurnBoundary'` passed after 1.702 seconds. Falsification: with `turnWillBegin()` deleted from the loop, the case failed after 30.220 seconds with 1 issue.
    - Acceptance criteria: all four hold. No file of a family package was changed, the timeout was not raised, and the assertion is stronger than the one it replaces — the whole path list, in place of one `contains` test.
    - next: `/review`.
  timestamp: 2026-09-07T17:47:19.119867+00:00
- actor: claude-code
  id: 01m1yg2shawdj59vat67y4fxfz
  text: |
    ### review — findings
    - evidence: `review sha HEAD~1..HEAD` gave 0 findings from 7 validator attempts. The judgement of the test shape gave 2 findings: `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:450`, `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:452`.
    - next: drive the catalog change from the case, in place of the timer of the scripted server. Then the loop does not depend on the clock, and the order of the reads around `turnWillBegin()` is sure.
  timestamp: 2026-09-07T17:53:58.826848+00:00
- actor: claude-code
  id: 01m1yg3nvkfdg1d6x26ve5tcm3
  text: |
    ### finish iteration 1 — findings

    - implement: changed — 1 file, `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift`. The case records the greeter's state at its first read, then loops: read, read again with no turn boundary between the two reads, `turnWillBegin()`, read again. It stops when the greeter's state is the opposite of the first read.
    - test: three `swift test` runs in sequence, each 428 tests in 45 suites passed with 1 known issue, the `withKnownIssue` at `Tests/FoundationModelsACPAgentTests/Support/HarnessSmokeTests.swift:239`, and no other issue. The filtered run passed after 1.702 seconds. Falsification: with `await runCode.turnWillBegin()` deleted, the case failed after 30.220 seconds, so it can still fail.
    - commit: `62c4017` `test(mcp): stop the staged tool-list case racing the server timer`
    - review: findings — `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:450`, `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:452`

    Both findings have one cause: the case rides the scripted server's timer in place of driving the catalog change itself. The named fix is to drive the change from the case, as `aReconnectStagesARebuildAndAnUnchangedCatalogStagesNothing` does at lines 506 and 507 of the same file.
  timestamp: 2026-09-07T17:54:27.827515+00:00
- actor: claude-code
  id: 01m1yg9nnn8edcv2rj43ntwtcj
  text: |
    Picked the card up again from `review`, moved it to `doing`, and started on the two findings.

    Research for the second attempt.

    Both findings name one cause: the case reads a catalog that a timer in the scripted server moves. The named fix is to make the case move the catalog itself.

    The old shape cannot do that. It builds the surface with `ToolCatalog.sessionSurface(context:)`, and that path takes its servers from the configuration. A configuration entry names a command, so the server is a subprocess (`mcp-test-server --mode dynamic`), and the case has no handle on the tool set of a subprocess. The timer is the only thing that moves it.

    The reconnect case shows the shape that works. It builds the same mounted surface by hand: `MultiTool.Builder()`, `withMCP(servers:)`, `buildRegistry()`, `makeSessionToolsAndStaging(librarian:)`, and `MCPComposition.startSurfaceRefresher(...)`. Its server runs in process behind a `TransportFactory`, so the case holds the `ScriptedServer` and can call `addEchoTool(named:)` and `emitToolListChanged()` on it at the moment it chooses.

    Facts read from `.build/checkouts/FoundationModelsMultitool` (read only, nothing changed there):
    - `ScriptedServer.addEchoTool(named:description:)` adds one tool, and `emitToolListChanged()` sends one `notifications/tools/list_changed`. Together they are the catalog change the case drives.
    - `ScriptedServer.startOnInMemoryPair()` starts the server on a fresh in-memory pair and gives back the client end, which is what a `TransportFactory` must return.
    - `MultiTool.Registry.makeSessionToolsAndStaging(librarian:)` gives `(tools, staging)`. With no search-tools support the tools are `[MultiTool, WaitTool]`, so the case still finds its `MultiTool` and can call `turnWillBegin()` on it.
    - `MultiTool+SandboxGlobals` installs `help()` unconditionally, so the `help()` snippet reads the surface of a bare builder registry too.

    One ordering hazard found while planning. `RecordingStaging.stage(_:)` records first and passes the registry to the mounted staging after. A case that watches `newestPaths` to learn that the rebuild was staged can therefore observe the record in the window before the mounted staging holds it, and a `turnWillBegin()` in that window would apply nothing. The two statements will be put in the opposite order, so a record that an observer can see means the mounted staging already holds the registry. The reconnect case only counts and reads paths, so the order makes no difference to it.
  timestamp: 2026-09-07T17:57:44.245987+00:00
- actor: claude-code
  id: 01m1ygwph3tbt6re7488yvtbhx
  text: |
    Both findings are corrected. The one cause they share is gone: the case no longer reads a catalog that a timer moves.

    What the case does now.

    1. It makes a `ScriptedServer` with the echo tool, and connects one `MCPServer` to it over a `TransportFactory` that serves an in-memory pair. The server is in process, so the case holds it.
    2. It mounts the surface and starts the refresher, and it waits for the connect snapshot to stage. It takes one turn boundary there, so the connect snapshot is applied and the next stage the case sees belongs to the change the case makes.
    3. It reads the surface. The echo path is in, and the extra path is out.
    4. It adds the extra tool to its own server and sends one `tools/list_changed`. This is the catalog change, and the case makes it.
    5. It waits until the recorded newest staged registry holds the extra path. The stage is now a fact, not a guess.
    6. It reads the surface again and expects the full path list did not move. This is the "staged, not applied" half, and it is sure because step 5 proved the stage came first.
    7. It runs `turnWillBegin()` and reads again. The extra path is in. This is the "applies at the boundary" half.

    No wall clock decides the result, and the order of the three reads around the change and the boundary is fixed by the code.

    Falsification, run against the final code.
    - Delete `await runCode.turnWillBegin()` before the last read: "recorded an issue at MCPCompositionTests.swift:509:13: Expectation failed: afterTheBoundary.contains(Self.boundaryExtraPath)", failed after 0.199 s with 1 issue.
    - Add a `turnWillBegin()` before the "staged, not applied" read, which is what an immediate apply would look like: "recorded an issue at MCPCompositionTests.swift:506:13: Expectation failed: whileStaged == beforeTheChange", failed after 0.182 s with 1 issue.
    Both edits were then removed, and the file was put back to the correct text. The case passes again after 0.188 s.

    Two more changes in the same file, each one the same cause removed from the whole file.

    - `RecordingStaging.stage(_:)` now passes the registry to the mounted staging first and records after. In the old order a case that watches `newestPaths` could read the record in the window before the mounted staging held the registry, and a turn boundary taken in that window would apply nothing. The doc comment states the reason. The reconnect case only counts and reads paths, so the order makes no difference to it.
    - The seven-line mount block was byte-identical in the two cases after the rewrite. It is now one harness helper, `mountSurface(of:)`, that returns the builder, the mounted tools and the recording staging. Both cases call it. This removes the duplicate the rewrite would have introduced.

    No other case in this file decides on a wall-clock delay. The one `Task.sleep` left is `noStageSettleDelay` in the reconnect case, which is the bounded wait for a negative — "a reconnect with the same catalog stages nothing more" — after a change that case drove itself. The review named that case as the shape to copy, so it stays.

    The case now runs in 0.19 s in place of 1.70 s, because it no longer spawns a subprocess and no longer waits out a stage timer.

    Nothing in `FoundationModelsMultitool`, `FoundationModelsRouter` or any other family package was changed. `.build/checkouts/...` was read for facts only. Nothing was pushed, and `stash@{0}` was not touched.
  timestamp: 2026-09-07T18:08:07.715538+00:00
- actor: claude-code
  id: 01m1ygwz26rr3ewabjdvvnzy5y
  text: |
    ### implement — changed
    - evidence: 1 file — `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift`. Three `swift test` runs in sequence: run 1 "Test run with 428 tests in 45 suites passed after 3.165 seconds with 1 known issue"; run 2 the same after 3.046 seconds; run 3 the same after 2.691 seconds. In each run the one known issue is `orderedSubsequenceAssertionChecksOrderWithGaps()` at `Tests/FoundationModelsACPAgentTests/Support/HarnessSmokeTests.swift:239`, and no other issue is recorded. `swift test --filter 'MCPCompositionTests'` passed, 15 tests in 1 suite, after 0.449 seconds. `swift test --filter 'MCPCompositionTests/aToolListChangeIsStagedAndAppliesOnlyAtTheNextTurnBoundary'` passed after 0.188 seconds. `swift build --build-tests` reports no diagnostic for the changed file.
    - falsification, against the final code: deleting the last `turnWillBegin()` gives "Expectation failed: afterTheBoundary.contains(Self.boundaryExtraPath)", failed after 0.199 s with 1 issue; adding a `turnWillBegin()` before the staged read gives "Expectation failed: whileStaged == beforeTheChange", failed after 0.182 s with 1 issue. Both edits were removed and the file was put back.
    - both review findings are corrected and checked, and the four acceptance criteria were each verified in this pass and checked.
    - next: `/review`.
  timestamp: 2026-09-07T18:08:16.454760+00:00
position_column: doing
position_ordinal: '80'
title: Stop MCPCompositionTests racing the dynamic server's 1500 ms timer
---
### What

`aToolListChangeIsStagedAndAppliesOnlyAtTheNextTurnBoundary()` fails in each full `swift test` run. The test wins a wall-clock race when it runs alone, and loses that race when the full suite runs. Remove the race.

1. The test is at `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:424`. Two assertions record an issue:
   - `MCPCompositionTests.swift:438` — `#expect(!initial.contains(Self.greeterPath))`
   - `MCPCompositionTests.swift:446` — `#expect(!beforeTick.contains(Self.greeterPath))`

2. Each full `swift test` run fails. Three runs gave 3, 3 and 4 issues, and one of those issues in each run is the known `withKnownIssue` at `Tests/FoundationModelsACPAgentTests/Support/HarnessSmokeTests.swift:239`. The other issues are always these two assertions.

3. The test passes when it runs alone. Three runs of `swift test --filter 'MCPCompositionTests/aToolListChangeIsStagedAndAppliesOnlyAtTheNextTurnBoundary'` each passed.

4. The cause is a fixed timer in the scripted server. `FoundationModelsMultitool`, in `Tests/Support/MCPTestServer/DynamicToolsetScenario.swift`, sets `dynamicToolsetStageDelayMilliseconds = 1500` (line 25) and schedules each stage with `scheduleMutation(after: dynamicToolsetStageDelay * (index + 1), stage)` (line 114). Stage 1 adds the `greeter` tool and sends `tools/list_changed`.

5. Thus the test has 1500 ms to start the subprocess, complete the MCP handshake and make its first `helpPaths` read. A full run keeps the machine busy, the first read comes after stage 1 fires, and the greeter is already in the catalog. The two assertions then fail.

6. Change the test to prove the property that its name states: a tool that the server adds becomes visible in the mounted surface only after `turnWillBegin()` runs. The test must not assume which stage has fired when it makes its first read.

7. Do not change `FoundationModelsMultitool`. It is a family package, and a change there needs a card on that project first. Make the correction in this repository.

### Test shape

- The test shows that a change to the server's catalog is not visible in the mounted surface until `turnWillBegin()` runs.
- The test does not use a wall-clock delay to decide if it passes.
- The full `swift test` run passes three times in sequence, with the one known issue at `HarnessSmokeTests.swift:239` and nothing else.
- The test still passes when it runs alone.

### Acceptance Criteria

- [x] `swift test` gives 428 tests in 45 suites with 1 known issue and no other issue, three runs in sequence.
- [x] `swift test --filter 'MCPCompositionTests/aToolListChangeIsStagedAndAppliesOnlyAtTheNextTurnBoundary'` passes.
- [x] No file of `FoundationModelsMultitool` or any other family package is changed.
- [x] The test asserts the staging property, not the absence of the greeter at a moment in time.

### Why

The root suite fails on each run, so no card can report a green root suite until this is corrected.

The failure appeared when the `FoundationModelsRouter` pin moved from `7360e20` to `efe0147` (the `release()` removal, Router cards `^fa7b61c`, `^5ph8cdm`, `^m8vj0jr` and `^m8pw5mz`). That pin changed the timing of the run. It did not change this code path: these two assertions read the MCP tool-list staging, which the Router's residency change does not reach.

## Review Findings (2026-09-07 12:55)

> Scope: `review sha HEAD~1..HEAD` — the diffs only. The engine fleet found no issue in 7 attempts. The two items below come from the judgement of the test shape that this card asks for.

- [x] `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:450` — the loop can stop only if the connect snapshot comes before the last stage of the scripted server. The scenario does three stages. They come 1500 ms, 3000 ms and 4500 ms after the server starts (`DynamicToolsetScenario.swift:114`, three stages at `:52`). Stage 1 adds the greeter, and stage 3 removes it. No stage comes after 4500 ms. If the connect snapshot comes after stage 3, then `greeterWasMounted` at line 445 is `false`, and no later stage can make the greeter come back. The loop then continues to the 30 s limit at line 173, and it records an issue. Thus the case still passes or fails on the clock. The time window is now 4500 ms in place of 1500 ms, but the card asks for a case that does not decide on a delay. Drive the catalog change from the case, as `aReconnectStagesARebuildAndAnUnchangedCatalogStagesNothing` does at lines 506 and 507, in place of the timer of the server.

- [x] `Tests/FoundationModelsACPAgentTests/MCPCompositionTests.swift:452` — this expectation can show nothing in the cycle that stops the loop. Each cycle has two parts. Part one holds the read at line 451, which comes after the read at line 454 of the cycle before. Part two holds `turnWillBegin()` at line 453 and the read at line 454. Line 452 shows that the surface holds still only if a stage comes in part one. If the stage comes in part two, the loop sees the change at the boundary and stops, and no expectation shows that the change waited for the boundary. The two parts have almost the same length, thus this occurs frequently. Drive the catalog change from the case, then read the surface, then call `turnWillBegin()`, then read again. This makes the order sure.

### Judgement of the test shape

The case is not weaker in all conditions, and it is not fully equal to its name.

- It is stronger than the case before it in one way. Line 452 compares the full path list, not only the greeter. Thus it finds any move of the surface between two boundaries, not only the greeter.
- The falsification check is good evidence. With `turnWillBegin()` removed, the case failed after 30.220 s. Thus the mounted surface did not move for 30 s while the server sent three `tools/list_changed` notifications. This is a direct proof of the staging property.
- It is weaker than its name in one way. The two items above show the two conditions in which the case can stop, or fail, for a reason that is not the staging property.
