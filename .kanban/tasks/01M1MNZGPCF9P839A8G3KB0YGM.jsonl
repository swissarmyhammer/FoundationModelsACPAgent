{"id":"01M1MNZGPCY2125CV5A6PP5H3R","timestamp":"2026-09-03T22:24:38.604672Z","op":"create","item_id":"01M1MNZGPCF9P839A8G3KB0YGM","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,59 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M1MNYFW81216M57PS9NDZKBE\n+position_column: todo\n+position_ordinal: '8580'\n+title: 'acp subcommand: move the stdio server into the command tree'\n+---\n+## What\n+\n+cli-plan.md §4 and §5.3. The stdio server exists today as the whole body\n+of the old `Examples/acp-agent/main.swift`. Move it into the `Acp`\n+subcommand with no behavior change.\n+\n+In `Sources/acp-agent/`, split the present `main.swift` body into:\n+\n+- `AgentComposition.swift` — the shared build steps that both `Acp` and\n+  `Run` need: the `DotfolderName`, the `ConfigurationLoader` load, the\n+  `Router` with a `LiveModelLoader`, and the `RoutedACPAgent`\n+  construction. One function that returns the composed agent.\n+- `AcpCommand.swift` — the `Acp` subcommand. It calls the composition,\n+  opens `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n+  binds the connection, and holds the process open until stdin ends.\n+\n+Keep the two protocol MUSTs of `plan.md` §17: stdout carries ndJSON\n+frames only, and a shell child never inherits stdout. `acp` mode never\n+reads stdin for a prompt, because stdin is the wire (§5.5).\n+\n+There is **no `--cwd`** on `acp`. The client gives the working directory\n+with each `session/new`, and a flag would fight the protocol (§5.10).\n+\n+- [ ] Extract `AgentComposition.swift`\n+- [ ] Write `AcpCommand.swift` over the extracted composition\n+- [ ] Keep the keep-alive and the stdin-EOF lifecycle\n+- [ ] Confirm `acp` declares no `--cwd`\n+\n+## Acceptance Criteria\n+\n+- [ ] `swift run acp-agent acp` serves ACP over stdio, as the old binary\n+      did.\n+- [ ] The gated tier-3 `StdioContractTests` passes without any change to\n+      the test, except the command it spawns gaining the `acp` word.\n+- [ ] `Acp` has no `--cwd` option: `acp-agent acp --cwd /tmp` is a usage\n+      error.\n+- [ ] No log line reaches stdout in `acp` mode.\n+\n+## Tests\n+\n+- [ ] Update `IntegrationTests/Tests/IntegrationTests/StdioContractTests.swift`\n+      to spawn `acp-agent acp` in place of bare `acp-agent`. It keeps\n+      every present assertion, and it still proves that stdout holds\n+      only ndJSON.\n+- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`: a\n+      `--cwd` on `acp` is a usage error, exit 2.\n+- [ ] `swift test` passes, and the gated tier-3 suite passes locally.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,59 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M1MNYFW81216M57PS9NDZKBE\n-position_column: todo\n-position_ordinal: '8580'\n-title: 'acp subcommand: move the stdio server into the command tree'\n----\n-## What\n-\n-cli-plan.md §4 and §5.3. The stdio server exists today as the whole body\n-of the old `Examples/acp-agent/main.swift`. Move it into the `Acp`\n-subcommand with no behavior change.\n-\n-In `Sources/acp-agent/`, split the present `main.swift` body into:\n-\n-- `AgentComposition.swift` — the shared build steps that both `Acp` and\n-  `Run` need: the `DotfolderName`, the `ConfigurationLoader` load, the\n-  `Router` with a `LiveModelLoader`, and the `RoutedACPAgent`\n-  construction. One function that returns the composed agent.\n-- `AcpCommand.swift` — the `Acp` subcommand. It calls the composition,\n-  opens `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n-  binds the connection, and holds the process open until stdin ends.\n-\n-Keep the two protocol MUSTs of `plan.md` §17: stdout carries ndJSON\n-frames only, and a shell child never inherits stdout. `acp` mode never\n-reads stdin for a prompt, because stdin is the wire (§5.5).\n-\n-There is **no `--cwd`** on `acp`. The client gives the working directory\n-with each `session/new`, and a flag would fight the protocol (§5.10).\n-\n-- [ ] Extract `AgentComposition.swift`\n-- [ ] Write `AcpCommand.swift` over the extracted composition\n-- [ ] Keep the keep-alive and the stdin-EOF lifecycle\n-- [ ] Confirm `acp` declares no `--cwd`\n-\n-## Acceptance Criteria\n-\n-- [ ] `swift run acp-agent acp` serves ACP over stdio, as the old binary\n-      did.\n-- [ ] The gated tier-3 `StdioContractTests` passes without any change to\n-      the test, except the command it spawns gaining the `acp` word.\n-- [ ] `Acp` has no `--cwd` option: `acp-agent acp --cwd /tmp` is a usage\n-      error.\n-- [ ] No log line reaches stdout in `acp` mode.\n-\n-## Tests\n-\n-- [ ] Update `IntegrationTests/Tests/IntegrationTests/StdioContractTests.swift`\n-      to spawn `acp-agent acp` in place of bare `acp-agent`. It keeps\n-      every present assertion, and it still proves that stdout holds\n-      only ndJSON.\n-- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`: a\n-      `--cwd` on `acp` is a usage error, exit 2.\n-- [ ] `swift test` passes, and the gated tier-3 suite passes locally.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M1MRBY5HTW5APWB738NE8P0H","timestamp":"2026-09-03T23:06:22.769709Z","op":"update","item_id":"01M1MNZGPCF9P839A8G3KB0YGM","forward_patch":"--- original\n+++ modified\n@@ -9,51 +9,62 @@\n ---\n ## What\n\n-cli-plan.md §4 and §5.3. The stdio server exists today as the whole body\n-of the old `Examples/acp-agent/main.swift`. Move it into the `Acp`\n-subcommand with no behavior change.\n-\n-In `Sources/acp-agent/`, split the present `main.swift` body into:\n+cli-plan.md §4 and §5.3. `9ndzkbe` already extracted\n+`AgentComposition.swift` and already passed the `acp` argument in the\n+tier-3 spawn. This card fills the `Acp` subcommand body.\n\n-- `AgentComposition.swift` — the shared build steps that both `Acp` and\n-  `Run` need: the `DotfolderName`, the `ConfigurationLoader` load, the\n-  `Router` with a `LiveModelLoader`, and the `RoutedACPAgent`\n-  construction. One function that returns the composed agent.\n-- `AcpCommand.swift` — the `Acp` subcommand. It calls the composition,\n-  opens `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n-  binds the connection, and holds the process open until stdin ends.\n+In `Sources/acp-agent/AcpCommand.swift`:\n\n-Keep the two protocol MUSTs of `plan.md` §17: stdout carries ndJSON\n-frames only, and a shell child never inherits stdout. `acp` mode never\n-reads stdin for a prompt, because stdin is the wire (§5.5).\n+- Call the shared `AgentComposition`.\n+- Open `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n+  bind the connection into the agent, and hold the process open until\n+  stdin ends. There is no teardown handshake: the client owns the\n+  lifecycle (`plan.md` §17).\n+- Keep the two protocol MUSTs: stdout carries ndJSON frames only, and a\n+  shell child never inherits stdout.\n+- `acp` mode never reads stdin for a prompt — stdin is the wire (§5.5).\n+- **No `--cwd` on `acp`.** The client gives the working directory with\n+  each `session/new`, and a flag would fight the protocol (§5.10).\n\n-There is **no `--cwd`** on `acp`. The client gives the working directory\n-with each `session/new`, and a flag would fight the protocol (§5.10).\n+**A note on the test gate.** There is no `ACP_TIER3` environment gate.\n+`IntegrationTests/Tests/FoundationModelsACPAgentIntegrationTests/Support/TierThreeFixture.swift`\n+says it plainly: *\"The suites carry no gate. This package is the gate:\n+the root `swift test` never sees these targets, and `swift test\n+--package-path IntegrationTests` runs them.\"* Use that boundary, and use\n+the real directory name — `FoundationModelsACPAgentIntegrationTests`,\n+not `IntegrationTests`.\n\n-- [ ] Extract `AgentComposition.swift`\n-- [ ] Write `AcpCommand.swift` over the extracted composition\n-- [ ] Keep the keep-alive and the stdin-EOF lifecycle\n-- [ ] Confirm `acp` declares no `--cwd`\n+- [ ] `AcpCommand.swift` over the shared composition\n+- [ ] The keep-alive and the stdin-EOF lifecycle\n+- [ ] `acp` declares no `--cwd`\n+- [ ] The per-session project layer test below\n\n ## Acceptance Criteria\n\n - [ ] `swift run acp-agent acp` serves ACP over stdio, as the old binary\n       did.\n-- [ ] The gated tier-3 `StdioContractTests` passes without any change to\n-      the test, except the command it spawns gaining the `acp` word.\n-- [ ] `Acp` has no `--cwd` option: `acp-agent acp --cwd /tmp` is a usage\n-      error.\n+- [ ] `swift test --package-path IntegrationTests` passes, with\n+      `StdioContractTests` unchanged from `9ndzkbe`.\n+- [ ] `acp-agent acp --cwd /tmp` is a usage error, exit 2.\n - [ ] No log line reaches stdout in `acp` mode.\n\n ## Tests\n\n-- [ ] Update `IntegrationTests/Tests/IntegrationTests/StdioContractTests.swift`\n-      to spawn `acp-agent acp` in place of bare `acp-agent`. It keeps\n-      every present assertion, and it still proves that stdout holds\n-      only ndJSON.\n-- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`: a\n+- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`:\n       `--cwd` on `acp` is a usage error, exit 2.\n-- [ ] `swift test` passes, and the gated tier-3 suite passes locally.\n+- [ ] **cli-plan §9's per-session layer row, which no other card owns.**\n+      An in-process test opens two sessions against one agent, with two\n+      temporary directories that hold different `config.yaml` files, and\n+      asserts each session resolved its **own** project layer — and that\n+      neither took the process working directory. This proves the §5.10\n+      \"two loads\" rule.\n+- [ ] **cli-plan §9's same-answer row, in process.** With\n+      `ACP_AGENT_STUB_MODEL=1`, a turn driven over\n+      `InMemoryTransport.pair()` and a turn driven over a stdio pair give\n+      byte-identical answer text. This is an ordinary test, not a\n+      spawned-binary one.\n+- [ ] `swift test` and `swift test --package-path IntegrationTests` both\n+      pass.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -9,62 +9,51 @@\n ---\n ## What\n\n-cli-plan.md §4 and §5.3. `9ndzkbe` already extracted\n-`AgentComposition.swift` and already passed the `acp` argument in the\n-tier-3 spawn. This card fills the `Acp` subcommand body.\n+cli-plan.md §4 and §5.3. The stdio server exists today as the whole body\n+of the old `Examples/acp-agent/main.swift`. Move it into the `Acp`\n+subcommand with no behavior change.\n\n-In `Sources/acp-agent/AcpCommand.swift`:\n+In `Sources/acp-agent/`, split the present `main.swift` body into:\n\n-- Call the shared `AgentComposition`.\n-- Open `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n-  bind the connection into the agent, and hold the process open until\n-  stdin ends. There is no teardown handshake: the client owns the\n-  lifecycle (`plan.md` §17).\n-- Keep the two protocol MUSTs: stdout carries ndJSON frames only, and a\n-  shell child never inherits stdout.\n-- `acp` mode never reads stdin for a prompt — stdin is the wire (§5.5).\n-- **No `--cwd` on `acp`.** The client gives the working directory with\n-  each `session/new`, and a flag would fight the protocol (§5.10).\n+- `AgentComposition.swift` — the shared build steps that both `Acp` and\n+  `Run` need: the `DotfolderName`, the `ConfigurationLoader` load, the\n+  `Router` with a `LiveModelLoader`, and the `RoutedACPAgent`\n+  construction. One function that returns the composed agent.\n+- `AcpCommand.swift` — the `Acp` subcommand. It calls the composition,\n+  opens `AgentSideConnection(stream: .stdio, logger: .standardError)`,\n+  binds the connection, and holds the process open until stdin ends.\n\n-**A note on the test gate.** There is no `ACP_TIER3` environment gate.\n-`IntegrationTests/Tests/FoundationModelsACPAgentIntegrationTests/Support/TierThreeFixture.swift`\n-says it plainly: *\"The suites carry no gate. This package is the gate:\n-the root `swift test` never sees these targets, and `swift test\n---package-path IntegrationTests` runs them.\"* Use that boundary, and use\n-the real directory name — `FoundationModelsACPAgentIntegrationTests`,\n-not `IntegrationTests`.\n+Keep the two protocol MUSTs of `plan.md` §17: stdout carries ndJSON\n+frames only, and a shell child never inherits stdout. `acp` mode never\n+reads stdin for a prompt, because stdin is the wire (§5.5).\n\n-- [ ] `AcpCommand.swift` over the shared composition\n-- [ ] The keep-alive and the stdin-EOF lifecycle\n-- [ ] `acp` declares no `--cwd`\n-- [ ] The per-session project layer test below\n+There is **no `--cwd`** on `acp`. The client gives the working directory\n+with each `session/new`, and a flag would fight the protocol (§5.10).\n+\n+- [ ] Extract `AgentComposition.swift`\n+- [ ] Write `AcpCommand.swift` over the extracted composition\n+- [ ] Keep the keep-alive and the stdin-EOF lifecycle\n+- [ ] Confirm `acp` declares no `--cwd`\n\n ## Acceptance Criteria\n\n - [ ] `swift run acp-agent acp` serves ACP over stdio, as the old binary\n       did.\n-- [ ] `swift test --package-path IntegrationTests` passes, with\n-      `StdioContractTests` unchanged from `9ndzkbe`.\n-- [ ] `acp-agent acp --cwd /tmp` is a usage error, exit 2.\n+- [ ] The gated tier-3 `StdioContractTests` passes without any change to\n+      the test, except the command it spawns gaining the `acp` word.\n+- [ ] `Acp` has no `--cwd` option: `acp-agent acp --cwd /tmp` is a usage\n+      error.\n - [ ] No log line reaches stdout in `acp` mode.\n\n ## Tests\n\n-- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`:\n+- [ ] Update `IntegrationTests/Tests/IntegrationTests/StdioContractTests.swift`\n+      to spawn `acp-agent acp` in place of bare `acp-agent`. It keeps\n+      every present assertion, and it still proves that stdout holds\n+      only ndJSON.\n+- [ ] `Tests/FoundationModelsACPAgentTests/CLIParsingTests.swift`: a\n       `--cwd` on `acp` is a usage error, exit 2.\n-- [ ] **cli-plan §9's per-session layer row, which no other card owns.**\n-      An in-process test opens two sessions against one agent, with two\n-      temporary directories that hold different `config.yaml` files, and\n-      asserts each session resolved its **own** project layer — and that\n-      neither took the process working directory. This proves the §5.10\n-      \"two loads\" rule.\n-- [ ] **cli-plan §9's same-answer row, in process.** With\n-      `ACP_AGENT_STUB_MODEL=1`, a turn driven over\n-      `InMemoryTransport.pair()` and a turn driven over a stdio pair give\n-      byte-identical answer text. This is an ordinary test, not a\n-      spawned-binary one.\n-- [ ] `swift test` and `swift test --package-path IntegrationTests` both\n-      pass.\n+- [ ] `swift test` passes, and the gated tier-3 suite passes locally.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
