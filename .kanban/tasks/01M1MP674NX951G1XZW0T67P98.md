---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m1xf5jzvvhna6qcr6qszxc7m
  text: |
    ### The Router cancellation question — answered: NO

    `router.resolve(profile:reporting:)` does NOT honour Task cancellation.

    Evidence, read at the pinned revision in
    `.build/checkouts/FoundationModelsRouter`:

    - `Sources/FoundationModelsRouter/Router.swift` holds zero occurrences of
      `Task.checkCancellation()`, `Task.isCancelled` and
      `withTaskCancellationHandler`.
    - The same count is zero in every file of `Resolution/` (`JointFit`,
      `LiveModelLoader`, `ModelLoader`, `PinnedDateTokenizerLoader`,
      `ResolutionProgress`, `SlotResolution`), of `Sizing/` (`Footprint`,
      `HostProfile`, `RepoMetadata`), of `Core/` and of `Concurrency/`
      (`AsyncSemaphore`, `RaceGate`, `ResidentModelGates`,
      `SerialAsyncChain`).
    - Every cancellation check the package has is in `Session/`
      (`RoutedSessionActorTurnExecution`, `RoutedSessionActorGeneration`,
      `RoutedSessionActorCompaction`) and in `Hosting/ToolRun.swift` — the
      turn path, not the resolve path.
    - `AsyncSemaphore` is the single-flight gate of the resolve. It has no
      cancellation handler, so a cancelled task that waits on it keeps
      waiting.

    A second and independent reason, in this repository: the resolve runs
    inside `RoutedACPAgent.init`, which `AgentComposition.compose` awaits,
    which `Run.perform(environment:into:)` awaits BEFORE `RunTurn.answer`
    pairs the two transport ends. During a download there is no ACP
    connection and no session, so `session/cancel` has no addressee. The
    §5.9 first-signal path cannot even be spoken during a resolution.

    ### What this card does about it

    - The download case is dropped from this card. This card covers the
      turn only: a `SIGINT` during a `session/prompt`.
    - Card `^54ay5s0` ("Router: make resolve honour Task cancellation, so
      Ctrl-C can stop a download") carries the download case. cli-plan.md
      §5.9 loses the download paragraph in this card, and it returns when
      the Router card lands.
  timestamp: 2026-09-07T08:18:47.419503+00:00
depends_on:
- 01M1MP01P7SV7C2S0S8QZ0A60T
position_column: todo
position_ordinal: '9180'
title: 'Interrupt: Ctrl-C sends session/cancel and exits 4'
---
### What

cli-plan.md §5.9. `Ctrl-C` must not kill the process.

In `Sources/acp-agent/InterruptHandler.swift`:

1. A `DispatchSourceSignal` for `SIGINT`, with the default disposition
   ignored so the process does not die. **The handler body only sets an
   atomic flag and resumes a continuation** — a signal handler must be
   async-signal-safe, so it calls nothing else. The real work happens on
   a normal task that waits on that continuation.
2. First signal: send `session/cancel`, wait for the `cancelled` stop
   reason, print the text that already arrived, exit 4.
3. Second signal: end the process at once.

### The download case is dropped — the answer is recorded

The Router cancellation question is answered in a task comment: **NO**.
`router.resolve(profile:reporting:)` does not honour Task cancellation.
`Router.swift`, `Resolution/`, `Sizing/`, `Core/` and `Concurrency/`
hold no `Task.checkCancellation()`, no `Task.isCancelled` and no
`withTaskCancellationHandler`. The resolve also runs before the wire
opens, so `session/cancel` has no addressee during a download.

So this card covers the turn only. Card `^54ay5s0` ("Router: make
resolve honour Task cancellation, so Ctrl-C can stop a download")
carries the download case, and cli-plan.md §5.9 loses its download
paragraph here.

- [x] Confirm Router's resolve cancellation, and record the answer
- [x] Drop the §5.9 download paragraph from cli-plan.md, and point it at
      the Router card
- [x] The `SIGINT` source; the handler body only sets a flag
- [x] First signal: cancel, wait, print, exit 4
- [x] Second signal: end at once

### Acceptance Criteria

- [x] A `SIGINT` during a turn gives exit 4, and the text that already
      arrived is on stdout.
- [x] A second `SIGINT` ends the process inside a named time limit.
- [x] A `session/cancel` reached the agent before the exit.
- [x] The `DispatchSourceSignal` handler body sets a flag and calls
      nothing else.
- [x] The Router cancellation question is answered in a task comment,
      and this card's scope matches the answer.

### Tests

The spawned-binary tests belong in the **nested** package, because
`BuiltProductLocator` lives there and cli-plan §9 puts the interrupt
test at that tier. Use `ACP_AGENT_STUB_MODEL=1` so the child is
deterministic and needs no weights.

- [x] `IntegrationTests/Tests/FoundationModelsACPAgentIntegrationTests/InterruptTests.swift`:
      spawn the built binary on a slow scripted turn, send `SIGINT`,
      assert exit 4 and the partial text.
- [x] A second-`SIGINT` test with a time bound.
- [x] An in-process test in `Tests/FoundationModelsACPAgentTests/`
      asserts the agent end of the wire received `session/cancel`.
- [x] A **source-level** test that the handler closure body contains
      only the flag set — this replaces the unassertable
      "calls nothing that is not async-signal-safe".
- [x] `swift test` and `swift test --package-path IntegrationTests` both
      pass.

### Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.
