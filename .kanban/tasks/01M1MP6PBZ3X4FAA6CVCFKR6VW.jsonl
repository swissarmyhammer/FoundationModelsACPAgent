{"id":"01M1MP6PC0NS1NVQV6FKA3M463","timestamp":"2026-09-03T22:28:33.792491Z","op":"create","item_id":"01M1MP6PBZ3X4FAA6CVCFKR6VW","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,70 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M1MNXE777J4XA3NJTP483A8W\n+- 01M1MNXY19R8HPEMNGF2WXB0G6\n+position_column: todo\n+position_ordinal: '9280'\n+title: 'Client package: the acp-client binary, N1 to N6'\n+---\n+### What\n+\n+Upstream work in `../FoundationModelsACPClient`, not in this repository.\n+The full specification is that repository's `cli-plan.md`, milestones N1\n+to N6. This card tracks it here, because the tier-3 interop test of this\n+package depends on the binary.\n+\n+`acp-client` is a command-line client for **any** ACP v2 agent:\n+\n+```\n+acp-client run <prompt> -- <agent-command> [agent-args...]\n+acp-client probe -- <agent-command> [agent-args...]\n+acp-client doctor -- <agent-command> [agent-args...]\n+```\n+\n+Everything after `--` is the agent command with its own arguments, so\n+the binary never splits a command string into words. `--frames` writes\n+every ndJSON message to stderr in both directions, and that option is\n+why the binary exists.\n+\n+It links the client package, the wire, `ArgumentParser` and Noora. It\n+must never import Router, this package, MCP, or the FoundationModels\n+framework — that import rule is what makes the \"a client, not *our*\n+client\" claim real.\n+\n+It is an executable **product**, so this package can depend on it and\n+spawn it beside its own binaries.\n+\n+- [ ] N1: the target, the product, the subcommand tree\n+- [ ] N2: `run`, and the prompt, stdout, stderr and exit-code rules\n+- [ ] N3: `--frames`\n+- [ ] N4: `probe`\n+- [ ] N5: `doctor` (its §10)\n+- [ ] N6: `--timeout`, the interrupt, and the reaping tests\n+- [ ] Merge to `main` in the client repository\n+\n+### Acceptance Criteria\n+\n+- [ ] `acp-client run \"hi\" -- <stub agent>` prints only the answer on\n+      stdout.\n+- [ ] A missing `--` gives a usage error and exit 2.\n+- [ ] `--frames` writes the messages to stderr, and stdout stays clean.\n+- [ ] No agent process outlives a run, in success, failure, timeout and\n+      interrupt.\n+- [ ] The exit codes match this package's table (0, 1, 2, 3, 4, 5, 124).\n+- [ ] The change is on the client `main`, because this package tracks\n+      the `main` branch and not a version.\n+\n+### Tests\n+\n+- [ ] Every row of the client plan's §14 test table, in that repository.\n+- [ ] `swift test` in `../FoundationModelsACPClient` passes.\n+\n+### Blocked by\n+\n+The Extras `Doctorable` module, for N5 only. N1 to N4 and N6 are free of\n+it, so start them at once.\n+\n+### Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,70 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M1MNXE777J4XA3NJTP483A8W\n-- 01M1MNXY19R8HPEMNGF2WXB0G6\n-position_column: todo\n-position_ordinal: '9280'\n-title: 'Client package: the acp-client binary, N1 to N6'\n----\n-### What\n-\n-Upstream work in `../FoundationModelsACPClient`, not in this repository.\n-The full specification is that repository's `cli-plan.md`, milestones N1\n-to N6. This card tracks it here, because the tier-3 interop test of this\n-package depends on the binary.\n-\n-`acp-client` is a command-line client for **any** ACP v2 agent:\n-\n-```\n-acp-client run <prompt> -- <agent-command> [agent-args...]\n-acp-client probe -- <agent-command> [agent-args...]\n-acp-client doctor -- <agent-command> [agent-args...]\n-```\n-\n-Everything after `--` is the agent command with its own arguments, so\n-the binary never splits a command string into words. `--frames` writes\n-every ndJSON message to stderr in both directions, and that option is\n-why the binary exists.\n-\n-It links the client package, the wire, `ArgumentParser` and Noora. It\n-must never import Router, this package, MCP, or the FoundationModels\n-framework — that import rule is what makes the \"a client, not *our*\n-client\" claim real.\n-\n-It is an executable **product**, so this package can depend on it and\n-spawn it beside its own binaries.\n-\n-- [ ] N1: the target, the product, the subcommand tree\n-- [ ] N2: `run`, and the prompt, stdout, stderr and exit-code rules\n-- [ ] N3: `--frames`\n-- [ ] N4: `probe`\n-- [ ] N5: `doctor` (its §10)\n-- [ ] N6: `--timeout`, the interrupt, and the reaping tests\n-- [ ] Merge to `main` in the client repository\n-\n-### Acceptance Criteria\n-\n-- [ ] `acp-client run \"hi\" -- <stub agent>` prints only the answer on\n-      stdout.\n-- [ ] A missing `--` gives a usage error and exit 2.\n-- [ ] `--frames` writes the messages to stderr, and stdout stays clean.\n-- [ ] No agent process outlives a run, in success, failure, timeout and\n-      interrupt.\n-- [ ] The exit codes match this package's table (0, 1, 2, 3, 4, 5, 124).\n-- [ ] The change is on the client `main`, because this package tracks\n-      the `main` branch and not a version.\n-\n-### Tests\n-\n-- [ ] Every row of the client plan's §14 test table, in that repository.\n-- [ ] `swift test` in `../FoundationModelsACPClient` passes.\n-\n-### Blocked by\n-\n-The Extras `Doctorable` module, for N5 only. N1 to N4 and N6 are free of\n-it, so start them at once.\n-\n-### Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M1MR6RV3Q7S8G3EK1Z3WAH31","timestamp":"2026-09-03T23:03:33.475002Z","op":"update","item_id":"01M1MP6PBZ3X4FAA6CVCFKR6VW","forward_patch":"--- original\n+++ modified\n@@ -1,70 +1,75 @@\n ---\n assignees:\n - claude-code\n-depends_on:\n-- 01M1MNXE777J4XA3NJTP483A8W\n-- 01M1MNXY19R8HPEMNGF2WXB0G6\n+depends_on: []\n position_column: todo\n position_ordinal: '9280'\n-title: 'Client package: the acp-client binary, N1 to N6'\n+title: 'acp-client N1+N2: the target, the subcommand tree, and run'\n ---\n ### What\n\n-Upstream work in `../FoundationModelsACPClient`, not in this repository.\n-The full specification is that repository's `cli-plan.md`, milestones N1\n-to N6. This card tracks it here, because the tier-3 interop test of this\n-package depends on the binary.\n+Upstream work in `../FoundationModelsACPClient`. Its `cli-plan.md`\n+milestones **N1 and N2**. The later milestones are separate cards.\n\n-`acp-client` is a command-line client for **any** ACP v2 agent:\n+This card is the one that `--out-of-process` interop testing waits on,\n+so it must not be blocked by the doctor work.\n\n-```\n-acp-client run <prompt> -- <agent-command> [agent-args...]\n-acp-client probe -- <agent-command> [agent-args...]\n-acp-client doctor -- <agent-command> [agent-args...]\n-```\n+**N1 — the target and the tree:**\n+- A new `acp-client` executable **target and product**. A product, so\n+  this package can depend on it and spawn it beside its own binaries.\n+- Declare `apple/swift-argument-parser` from 1.8.0 and\n+  `github.com/tuist/Noora`, pinned to the same exact version the agent\n+  package pins.\n+- `Sources/acp-client/Terminal/TerminalRenderer.swift` — the client's\n+  own renderer, matching the agent's: a spinner, a progress bar and a\n+  table, on **stderr only**, silent when the destination is not a\n+  terminal. Take the destination and the terminal test as injected\n+  values, so both paths are automated.\n+- The subcommand tree: `run` (default), `probe`, `doctor`, `--help`,\n+  `--version`. `probe`, `doctor` are stubs that exit 1 here.\n\n-Everything after `--` is the agent command with its own arguments, so\n-the binary never splits a command string into words. `--frames` writes\n-every ndJSON message to stderr in both directions, and that option is\n-why the binary exists.\n-\n-It links the client package, the wire, `ArgumentParser` and Noora. It\n-must never import Router, this package, MCP, or the FoundationModels\n-framework — that import rule is what makes the \"a client, not *our*\n-client\" claim real.\n+**N2 — `run`:**\n+- `acp-client run <prompt> -- <agent-command> [args...]`. Everything\n+  after `--` is the agent command. The binary never splits a command\n+  string into words.\n+- With no `--`: usage error, exit 2, stdout empty.\n+- The prompt source, stdout, stderr and exit codes of the client plan\n+  §7 to §9.\n\n-It is an executable **product**, so this package can depend on it and\n-spawn it beside its own binaries.\n+It links this package, the wire, `ArgumentParser` and `Noora`. Never\n+Router, ACPAgent, MCP or the FoundationModels framework.\n\n-- [ ] N1: the target, the product, the subcommand tree\n-- [ ] N2: `run`, and the prompt, stdout, stderr and exit-code rules\n-- [ ] N3: `--frames`\n-- [ ] N4: `probe`\n-- [ ] N5: `doctor` (its §10)\n-- [ ] N6: `--timeout`, the interrupt, and the reaping tests\n+- [ ] N1: the target, the product, the two package dependencies\n+- [ ] N1: `TerminalRenderer`, with an injected destination and terminal test\n+- [ ] N1: the subcommand tree, with `probe` and `doctor` stubbed\n+- [ ] N2: `run` over `--`, and §7 to §9\n - [ ] Merge to `main` in the client repository\n\n ### Acceptance Criteria\n\n - [ ] `acp-client run \"hi\" -- <stub agent>` prints only the answer on\n-      stdout.\n-- [ ] A missing `--` gives a usage error and exit 2.\n-- [ ] `--frames` writes the messages to stderr, and stdout stays clean.\n-- [ ] No agent process outlives a run, in success, failure, timeout and\n-      interrupt.\n-- [ ] The exit codes match this package's table (0, 1, 2, 3, 4, 5, 124).\n-- [ ] The change is on the client `main`, because this package tracks\n-      the `main` branch and not a version.\n+      stdout, byte for byte.\n+- [ ] A missing `--` gives a usage error and exit 2, with stdout empty.\n+- [ ] The agent arguments after `--` reach the agent unchanged, flags\n+      included.\n+- [ ] `TerminalRenderer` writes zero bytes when its injected destination\n+      is not a terminal, and draws when it is.\n+- [ ] `TerminalRenderer.swift` is the only file under\n+      `Sources/acp-client/` that holds `import Noora`.\n+- [ ] The exit codes match the shared table: 0, 1, 2, 3, 4.\n+- [ ] The change is on the client `main`.\n\n ### Tests\n\n-- [ ] Every row of the client plan's §14 test table, in that repository.\n+- [ ] The subcommand tree, and the missing-`--` usage error.\n+- [ ] The agent arguments after `--` reach the stub agent unchanged.\n+- [ ] The prompt-source table of the client plan §7, each row.\n+- [ ] The stub agent over stdio: stdout is only the answer text, byte\n+      for byte.\n+- [ ] `TerminalRenderer` against an injected non-terminal destination\n+      (zero bytes) and an injected terminal one (draws).\n+- [ ] A source-level test: exactly one file holds `import Noora`.\n - [ ] `swift test` in `../FoundationModelsACPClient` passes.\n\n-### Blocked by\n-\n-The Extras `Doctorable` module, for N5 only. N1 to N4 and N6 are free of\n-it, so start them at once.\n-\n ### Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,75 +1,70 @@\n ---\n assignees:\n - claude-code\n-depends_on: []\n+depends_on:\n+- 01M1MNXE777J4XA3NJTP483A8W\n+- 01M1MNXY19R8HPEMNGF2WXB0G6\n position_column: todo\n position_ordinal: '9280'\n-title: 'acp-client N1+N2: the target, the subcommand tree, and run'\n+title: 'Client package: the acp-client binary, N1 to N6'\n ---\n ### What\n\n-Upstream work in `../FoundationModelsACPClient`. Its `cli-plan.md`\n-milestones **N1 and N2**. The later milestones are separate cards.\n+Upstream work in `../FoundationModelsACPClient`, not in this repository.\n+The full specification is that repository's `cli-plan.md`, milestones N1\n+to N6. This card tracks it here, because the tier-3 interop test of this\n+package depends on the binary.\n\n-This card is the one that `--out-of-process` interop testing waits on,\n-so it must not be blocked by the doctor work.\n+`acp-client` is a command-line client for **any** ACP v2 agent:\n\n-**N1 — the target and the tree:**\n-- A new `acp-client` executable **target and product**. A product, so\n-  this package can depend on it and spawn it beside its own binaries.\n-- Declare `apple/swift-argument-parser` from 1.8.0 and\n-  `github.com/tuist/Noora`, pinned to the same exact version the agent\n-  package pins.\n-- `Sources/acp-client/Terminal/TerminalRenderer.swift` — the client's\n-  own renderer, matching the agent's: a spinner, a progress bar and a\n-  table, on **stderr only**, silent when the destination is not a\n-  terminal. Take the destination and the terminal test as injected\n-  values, so both paths are automated.\n-- The subcommand tree: `run` (default), `probe`, `doctor`, `--help`,\n-  `--version`. `probe`, `doctor` are stubs that exit 1 here.\n+```\n+acp-client run <prompt> -- <agent-command> [agent-args...]\n+acp-client probe -- <agent-command> [agent-args...]\n+acp-client doctor -- <agent-command> [agent-args...]\n+```\n\n-**N2 — `run`:**\n-- `acp-client run <prompt> -- <agent-command> [args...]`. Everything\n-  after `--` is the agent command. The binary never splits a command\n-  string into words.\n-- With no `--`: usage error, exit 2, stdout empty.\n-- The prompt source, stdout, stderr and exit codes of the client plan\n-  §7 to §9.\n+Everything after `--` is the agent command with its own arguments, so\n+the binary never splits a command string into words. `--frames` writes\n+every ndJSON message to stderr in both directions, and that option is\n+why the binary exists.\n\n-It links this package, the wire, `ArgumentParser` and `Noora`. Never\n-Router, ACPAgent, MCP or the FoundationModels framework.\n+It links the client package, the wire, `ArgumentParser` and Noora. It\n+must never import Router, this package, MCP, or the FoundationModels\n+framework — that import rule is what makes the \"a client, not *our*\n+client\" claim real.\n+\n+It is an executable **product**, so this package can depend on it and\n+spawn it beside its own binaries.\n\n-- [ ] N1: the target, the product, the two package dependencies\n-- [ ] N1: `TerminalRenderer`, with an injected destination and terminal test\n-- [ ] N1: the subcommand tree, with `probe` and `doctor` stubbed\n-- [ ] N2: `run` over `--`, and §7 to §9\n+- [ ] N1: the target, the product, the subcommand tree\n+- [ ] N2: `run`, and the prompt, stdout, stderr and exit-code rules\n+- [ ] N3: `--frames`\n+- [ ] N4: `probe`\n+- [ ] N5: `doctor` (its §10)\n+- [ ] N6: `--timeout`, the interrupt, and the reaping tests\n - [ ] Merge to `main` in the client repository\n\n ### Acceptance Criteria\n\n - [ ] `acp-client run \"hi\" -- <stub agent>` prints only the answer on\n-      stdout, byte for byte.\n-- [ ] A missing `--` gives a usage error and exit 2, with stdout empty.\n-- [ ] The agent arguments after `--` reach the agent unchanged, flags\n-      included.\n-- [ ] `TerminalRenderer` writes zero bytes when its injected destination\n-      is not a terminal, and draws when it is.\n-- [ ] `TerminalRenderer.swift` is the only file under\n-      `Sources/acp-client/` that holds `import Noora`.\n-- [ ] The exit codes match the shared table: 0, 1, 2, 3, 4.\n-- [ ] The change is on the client `main`.\n+      stdout.\n+- [ ] A missing `--` gives a usage error and exit 2.\n+- [ ] `--frames` writes the messages to stderr, and stdout stays clean.\n+- [ ] No agent process outlives a run, in success, failure, timeout and\n+      interrupt.\n+- [ ] The exit codes match this package's table (0, 1, 2, 3, 4, 5, 124).\n+- [ ] The change is on the client `main`, because this package tracks\n+      the `main` branch and not a version.\n\n ### Tests\n\n-- [ ] The subcommand tree, and the missing-`--` usage error.\n-- [ ] The agent arguments after `--` reach the stub agent unchanged.\n-- [ ] The prompt-source table of the client plan §7, each row.\n-- [ ] The stub agent over stdio: stdout is only the answer text, byte\n-      for byte.\n-- [ ] `TerminalRenderer` against an injected non-terminal destination\n-      (zero bytes) and an injected terminal one (draws).\n-- [ ] A source-level test: exactly one file holds `import Noora`.\n+- [ ] Every row of the client plan's §14 test table, in that repository.\n - [ ] `swift test` in `../FoundationModelsACPClient` passes.\n\n+### Blocked by\n+\n+The Extras `Doctorable` module, for N5 only. N1 to N4 and N6 are free of\n+it, so start them at once.\n+\n ### Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
