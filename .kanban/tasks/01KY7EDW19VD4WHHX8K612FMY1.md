---
depends_on:
- 01KY7EDAAEV7M16J19CESQ54ZR
position_column: todo
position_ordinal: '8380'
title: 'ToolCatalog: well-known sections construct the built-in roster'
---
Plan §7 + head decision. The marked ADD-TOOLS-HERE location, relocated from the pre-pivot harness: files: → FoundationModelsFileTool (confined to the session cwd via PathGuard), shell: → FoundationModelsShelltool (ShellPolicy from the same stack layers). Presence enables; section body decodes as that tool package's own option type. Per-cwd construction (project layer differs per cwd → per-session tool sets through makeSession(tools:)). Reserved future names: codeContext:, multitool:, skills:, agents:. Cross-repo: needs Router jkdae4b (tools through makeSession) to actually run turns; catalog + construction unit-testable before that.