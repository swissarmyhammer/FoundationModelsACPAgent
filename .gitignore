.build/
.DS_Store

# Each family dependency tracks a branch. The lock file pins one revision on
# one machine, and no sibling package commits it.
Package.resolved

# Claude Code machine-local settings
.claude/settings.local.json

# kanban MCP server scratch logs
.kanban/*.log

# The score step of the bench writes these into the working directory. They
# are the result of one run on one machine, and a new run makes them again.
logs/
*.score_*.json
